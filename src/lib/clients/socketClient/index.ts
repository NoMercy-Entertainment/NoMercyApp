// import {Ref, toRaw} from 'vue';

export const useSocket = () => {
    // return toRaw(socket.value) as HubConnection
};

export const useSocketRef = () => {
    // return socket as Ref<HubConnection>
};

export const useSync = () => {
    // return toRaw(sync.value) as MusicSync
}
